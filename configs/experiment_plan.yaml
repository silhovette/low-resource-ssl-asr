# Full experiment plan
# Run with:  python scripts/run_experiments.py --plan configs/experiment_plan.yaml
#
# Phase 1: Baselines (already done)
# Phase 2: Layer sweep — fills in layers 0, 3, 9 to complete the curve
# Phase 3: Fine-tuning — frozen warm-up then unfreeze top layers
# Phase 4: LM rescoring — greedy train, beam+LM eval (train LM first!)

runs:
  # ---- Phase 1: Baselines (already completed) ----
  - configs/mel_1h_hf_cuda.yaml
  - configs/wav2vec2_1h_frozen_hf_cuda.yaml
  - configs/wav2vec2_1h_layer6_hf_cuda.yaml
  - configs/hubert_1h_frozen_hf_cuda.yaml

  # ---- Phase 2: Layer sweep (new) ----
  - configs/wav2vec2_1h_layer0.yaml
  - configs/wav2vec2_1h_layer3.yaml
  - configs/wav2vec2_1h_layer9.yaml

  # ---- Phase 3: Fine-tuning (new) ----
  - configs/wav2vec2_1h_finetune6.yaml
  - configs/hubert_1h_finetune6.yaml

  # ---- Phase 4: LM rescoring (new — requires lm/char_5gram.arpa) ----
  - configs/wav2vec2_1h_layer6_lm.yaml
  - configs/hubert_1h_lm.yaml
